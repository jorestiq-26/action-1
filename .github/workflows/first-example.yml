name: My First Workflow

on: push

jobs:
        first_job:
            runs-on: ubuntu-latest
            steps:
            - name: Checkout Repo
              uses: actions/checkout@v6
            
            - name: Welcome message
              run: echo "My First Git Hub Action Job"

            - name: List files
              run: ls 

            - name: Read files
              run: cat README.md